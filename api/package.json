{
  "name": "cgpt-api",
  "version": "0.0.1",
  "author": "Aslam Ahammed A",
  "license": "MIT",
  "scripts": {
    "serve": "cargo watch -x run",
    "build": "cargo build --release",
    "build:lambda": "cargo lambda build --features lambda --arm64 --release --output-format zip",
    "start-dev-db": "docker-compose -f docker-compose.dev.yml up"
  }
}
